// ! ||--------------------------------------------------------------------------------||
// ! ||                              function declaration                              ||
// ! ||--------------------------------------------------------------------------------||
console.log(soma(3, 4));
// console.log(sum(3, 4)); //ERROR -> "sum" is not defined
function soma(x, y) {
  return x + y;
}

// ! ||--------------------------------------------------------------------------------||
// ! ||                               function expression                              ||
// ! ||--------------------------------------------------------------------------------||
const sub = function (x, y) {
  return x - y;
};

// ! ||--------------------------------------------------------------------------------||
// ! ||                            named function expression                           ||
// ! ||--------------------------------------------------------------------------------||
const mult = function mult(x, y) {
  return x * y;
}; // Forma pouco usada
console.log("1)", mult(2, 4));
